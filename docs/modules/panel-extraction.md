# Panel Extraction

Scientific figures often consist of multiple sub-panels (e.g., A, B, C). This module separates them for individual analysis.

## Technology
- Uses a YOLO (You Only Look Once) object detection model trained on scientific figures.
- Identifies bounding boxes for panels and labels.
